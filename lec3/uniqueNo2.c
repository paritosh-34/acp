// Find all the unique no's (that only occur once) in the give input.

#include <stdio.h>

int main()
{
  int n, xorr = 0;
  scanf("%d", &n);

  int arr[n];
  for (int i = 0; i < n; i++)
  {
    scanf("%d", &arr[i]);
  }

  int ans1 = 0;
  for (int i = 0; i < n; i++)
  {
  }

  return 0;
}